<?php
// db_connect.php

// Define database connection parameters
define('DB_HOST', 'sql307.infinityfree.com');
define('DB_NAME', 'if0_40175666_emerald_microfinance'); // Based on your SQL file
define('DB_USER', 'if0_40175666'); // Change this to your actual username
define('DB_PASS', 'Toxicchill1'); // Change this to your actual password

/**
 * Establishes a PDO database connection.
 * @return PDO The PDO database connection object.
 */
function connectDB() {
    $dsn = 'mysql:host=' . DB_HOST . ';dbname=' . DB_NAME . ';charset=utf8mb4';
    $options = [
        PDO::ATTR_ERRMODE            => PDO::ERRMODE_EXCEPTION,
        PDO::ATTR_DEFAULT_FETCH_MODE => PDO::FETCH_ASSOC,
        PDO::ATTR_EMULATE_PREPARES   => false,
    ];
    try {
        return new PDO($dsn, DB_USER, DB_PASS, $options);
    } catch (\PDOException $e) {
        // Log the error (optional) and return a standard error response
        error_log("DB Connection Error: " . $e->getMessage());
        header('Content-Type: application/json');
        http_response_code(500);
        echo json_encode(['success' => false, 'message' => 'Database connection failed.']);
        exit;
    }
}
?>